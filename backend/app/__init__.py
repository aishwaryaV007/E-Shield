# 1. FILE PURPOSE: Marks backend/app as the root Python package for the E-Shield backend.
# 2. RESPONSIBILITIES:
#    - Expose the `app` package so submodules import as `app.<module>`.
#    - Optionally hold package-level version/metadata.
# 3. PLANNED CONTENTS: Package docstring; optional __version__.
# 4. INPUTS / OUTPUTS: Inputs: none. Outputs: none.
# 5. DEPENDS ON / USED BY: Imported by all backend submodules and the test suite.
