# E-Shield backend environment configuration.
# Copy to backend/.env and adjust as needed. Never commit the real .env.

# ── Local paths (relative to repo root) ──
DB_PATH=../data/eshield.sqlite
DATA_DIR=../data
MODEL_DIR=../models_cache

# ── API ──
CORS_ORIGINS=http://localhost:3000
LOG_LEVEL=INFO
