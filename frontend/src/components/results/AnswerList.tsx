/*
 1. FILE PURPOSE: Question-wise list of auto-graded answers for an evaluated sheet.
 2. RESPONSIBILITIES:
    - Fetch a script's evaluation (useResults), render an AnswerCard per question in order.
 3. PLANNED CONTENTS: AnswerList({ scriptId }) component.
 4. INPUTS / OUTPUTS: Inputs: script id. Outputs: a list of AnswerCard.
 5. DEPENDS ON / USED BY: hooks/useResults, results/AnswerCard; used by app/scripts/[id]/page.tsx.
*/
