/*
 1. FILE PURPOSE: PostCSS config enabling Tailwind CSS and autoprefixer.
 2. RESPONSIBILITIES:
    - Register the tailwindcss and autoprefixer plugins.
 3. PLANNED CONTENTS: module.exports.plugins = { tailwindcss, autoprefixer }.
 4. INPUTS / OUTPUTS: Inputs: raw CSS. Outputs: processed CSS.
 5. DEPENDS ON / USED BY: tailwindcss, autoprefixer; used by the Next build + globals.css.
*/
